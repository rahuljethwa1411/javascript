
//++++++++++   NUMBERS ++++++++++++//

const score = 100
console.log(score)

const balance = new Number(100)   // right way to check the n number of functions
console.log(balance)

console.log(balance.toString())  // converted num to string
console.log(balance.toFixed(3))  // used for e-commerce

//++++++++++   MATHS  ++++++++++++//

const min = 10
const max = 20

console.log(Math.floor(Math.random() * (max - min + 1)) + min )  // formula